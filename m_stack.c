#include "monty.h"
/**
 * f_stack - A function that prints the top of the stack
 * @head: The head of the stack
 * @counter: A line_number
 * Return: returns nothing
*/
void f_stack(stack_t **head, unsigned int counter)
{
	(void)head;
	(void)counter;
	bus.lifi = 0;
}
